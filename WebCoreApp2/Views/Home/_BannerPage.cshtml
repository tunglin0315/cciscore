@model IEnumerable<Banner>
@{
    string active = "active";
}
@for (int i = 0; i < Model.Count(); i++)
{
    <button data-target="#myCarousel" data-slide-to="@i" class="btn-outline-warning  @active" style="width:30px; margin-right:2px"></button>

    { active = ""; }
}