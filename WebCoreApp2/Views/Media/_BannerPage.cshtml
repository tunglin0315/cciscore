@model IEnumerable<Cartoon>
@{
    string active = "active";
}
@for (int i = 0; i < Model.Count(); i++)
{
    <button data-target="#myCarousel" data-slide-to="@i" class="btn-success btn-sm @active">@(i+1)</button>
     { active = ""; }
}